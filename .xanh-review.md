# Xanh AI Code Review

> Phân tích code tự động bởi Xanh + Groq Llama 3 AI.
> Mỗi ngày, hệ thống random chọn 1 file và đưa ra nhận xét.

---

### Review ngày 31/05/2026 22:00

**File:** `src/components/annotator/home/FeedbackSection.jsx`

### Potential Bugs
* Không kiểm tra kiểu dữ liệu của `feedbacks` trước khi sử dụng, có thể gây lỗi nếu `feedbacks` không phải là mảng.
* Không kiểm tra sự tồn tại của các thuộc tính `id`, `projectName`, `date`, `comment` trong từng phần tử của `feedbacks`.
* Không xử lý trường hợp `t("annotatorFeedback.title")` hoặc `t("annotatorFeedback.noFeedback")` trả về `undefined` hoặc `null`.

### Clean Code
* Tên biến `feedbacks` có thể được đổi thành `feedbackList` để rõ ràng hơn.
* Có thể tách phần render của `ListGroup` thành một component riêng để giảm độ phức tạp.
* Có thể sử dụng destructuring để lấy các thuộc tính của `item` trong vòng lặp `map`.

### Suggestions
* Thêm kiểm tra kiểu dữ liệu cho `feedbacks` bằng cách sử dụng `PropTypes` hoặc `TypeScript`.
```jsx
import PropTypes from 'prop-types';

FeedbackSection.propTypes = {
  feedbacks: PropTypes.arrayOf(PropTypes.shape({
    id: PropTypes.string.isRequired,
    projectName: PropTypes.string.isRequired,
    date: PropTypes.string.isRequired,
    comment: PropTypes.string.isRequired,
  })),
};
```
* Tách phần render của `ListGroup` thành một component riêng.
```jsx
const FeedbackItem = ({ item }) => {
  return (
    <ListGroup.Item key={item.id} className="p-3 border-bottom">
      <div className="d-flex justify-content-between mb-1">
        <span className="fw-bold small">{item.projectName}</span>
        <Badge bg="light" className="text-muted fw-normal border">
          <Clock size={10} /> {item.date}
        </Badge>
      </div>
      <p className="text-muted small mb-0">{item.comment}</p>
    </ListGroup.Item>
  );
};
```

---

### Review ngày 30/05/2026 22:44

**File:** `src/page/manager/review/ReviewAuditPage.jsx`

### Tiềm năng Bugs
* Logic lỗi trong hàm `handleProjectChange` khi gọi `Promise.all` mà không kiểm tra lỗi riêng biệt cho từng promise.
* Không kiểm tra null safety cho `projectDetail` trước khi truy cập vào các thuộc tính của nó.
* Không có xử lý lỗi khi gọi API trong hàm `handleAudit`.

### Vấn đề về Bảo mật
* Không có thông tin về việc sử dụng các bí mật (secret) trong mã nguồn.
* Không có thông tin về việc sử dụng các biện pháp bảo mật cho dữ liệu người dùng.

### Hiệu suất
* Sử dụng `useMemo` để tính toán `samplingStats` và `reviewerStats` có thể gây ra hiệu suất kém nếu dữ liệu lớn.
* Không có tối ưu hóa cho việc gọi API trong hàm `handleProjectChange`.

### Clean Code
* Mã nguồn có nhiều đoạn code lặp lại, ví dụ như việc sử dụng `toast.error` và `toast.success`.
* Một số biến có tên không rõ ràng, ví dụ như `p` trong hàm `handleProjectChange`.

### Đề xuất
* Sử dụng try-catch để xử lý lỗi khi gọi API trong hàm `handleAudit`.
* Tối ưu hóa việc gọi API trong hàm `handleProjectChange` bằng cách sử dụng caching hoặc pagination.
* Sử dụng các biến có tên rõ ràng và tránh lặp lại code.
```jsx
// Ví dụ về việc sử dụng try-catch để xử lý lỗi khi gọi API
try {
  await reviewAuditService.auditReview({
    reviewLogId: selectedTask.reviewLogId || selectedTask.id,
    isCorrectDecision,
    auditComment,
  });
  toast.success(t("reviewAudit.auditSuccess"));
} catch (error) {
  toast.error(error.response?.data?.message || t("reviewAudit.auditError"));
}
```

---

### Review ngày 30/05/2026 06:35

**File:** `src/hooks/useSignalRRefresh.test.js`

### Potential Bugs
* Hàm `subscribeMock` có thể trả về `undefined` nếu không được gọi với đúng số lượng tham số, dẫn đến lỗi khi gọi `unsubscribe`.
* Không có kiểm tra lỗi khi gọi `localStorage.setItem`, có thể gây ra lỗi nếu không có quyền truy cập vào bộ nhớ cục bộ.
* Hàm `notificationHandler` không được kiểm tra null trước khi gọi, có thể gây ra lỗi nếu `subscribeMock` không trả về hàm callback.

### Performance
* Hàm `beforeEach` được gọi trước mỗi test, có thể gây ra hiệu suất kém nếu có nhiều test.
* Sử dụng `vi.mock` có thể gây ra hiệu suất kém nếu không được cấu hình đúng.
* Hàm `render` được gọi nhiều lần, có thể gây ra hiệu suất kém nếu không được tối ưu hóa.

### Clean Code
* Tên biến `subscribeMock` và `toastApi` không rõ ràng, nên đổi thành tên biến có ý nghĩa hơn.
* Hàm `TestHarness` không có chức năng rõ ràng, nên đổi thành tên hàm có ý nghĩa hơn.
* Code có nhiều đoạn lặp lại, nên sử dụng DRY (Don't Repeat Yourself) để tối ưu hóa code.

### Suggestions
* Thêm kiểm tra lỗi khi gọi `localStorage.setItem` để đảm bảo quyền truy cập vào bộ nhớ cục bộ.
```javascript
try {
  localStorage.setItem("access_token", "token-123");
} catch (error) {
  console.error(error);
}
```
* Sử dụng `jest.spyOn` thay vì `vi.mock` để mock hàm `subscribe` và `toastApi`.
```javascript
const subscribeSpy = jest.spyOn(signalrManager, 'subscribe');
const toastSpy = jest.spyOn(toastApi, 'info');
```
* Tối ưu hóa code bằng cách sử dụng DRY và đổi tên biến có ý nghĩa hơn.
```javascript
const renderTestHarness = (props) => {
  return render(React.createElement(TestHarness, props));
};
```

---
